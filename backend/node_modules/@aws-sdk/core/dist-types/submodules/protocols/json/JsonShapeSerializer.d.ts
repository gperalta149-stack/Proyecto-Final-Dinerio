import { SerdeContextConfig } from "@aws-sdk/core/dist-types/submodules/protocols/ConfigurableSerdeContext";
import type { JsonSettings } from "@aws-sdk/core/dist-types/submodules/protocols/json/JsonCodec";
import type { Schema, ShapeSerializer } from "@smithy/types";
/**
 * @public
 */
export declare class JsonShapeSerializer extends SerdeContextConfig implements ShapeSerializer<string> {
    readonly settings: JsonSettings;
    private buffer;
    private rootSchema;
    constructor(settings: JsonSettings);
    write(schema: Schema, value: unknown): void;
    /**
     * @internal
     */
    writeDiscriminatedDocument(schema: Schema, value: unknown): void;
    flush(): string;
    private _write;
}
